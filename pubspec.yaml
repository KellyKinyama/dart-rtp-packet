name: dart_rtp_packet
description: A sample command-line application.
version: 1.0.0
repository: https://github.com/KellyKinyama/dart-rtp-packet

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  pointycastle: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
